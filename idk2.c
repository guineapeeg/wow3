// pthread_mutex_lock(&lock);

        // printf("Printing from thread %s whole array: \n", actualArguments->channelPath);

        // for(int moor = 0; moor<= integerCounter-1; moor++){
        //     printf("%f \n", integers[moor]);
        // }

        // //alpha counting
        // printf("\n Alpha: %f \n", actualArguments->amplification);
        // printf("\n Alpha counting: \n");
        // printf("%f \n", integers[0]);

        // for(int kounter = 1; kounter<= integerCounter-1; kounter++){
        //     float newValue;
        //     float newValue2;

        //     float alpha = actualArguments->amplification;
        //     newValue = alpha * integers[kounter];
        //     newValue2 = (1-alpha) * integers[kounter-1];
        //     newValue = newValue + newValue2;
            
        //     integers[kounter] = newValue;
        // }

        //  for(int moor = 1; moor<= integerCounter-1; moor++){
        //     printf("%f \n", integers[moor]);
        // }

        // pthread_mutex_unlock(&lock);